import express, { Request, Response } from "express";

const router = express.Router();

// TODO: implement stats routes (/stats/xxx) to visualize voting results and participation

export default router;
